{
  "version": 2,
  "buildCommand": "npm run vercel-build && vercel build",
  "builds": [
    {
      "src": "dist/index.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/dist" }]
}
